# Ambiente do projeto "Efeito Dominó no Mercado de Transferências" (Grupo 02 - UFMG)
# Python 3.12.13 (venv: .venv). Versões fixadas no ambiente em que os notebooks foram executados.
#
# Reproduzir do zero:
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Observação: econml (CausalForestDML) NÃO é instalável neste ambiente
# (Python 3.12 + NumPy 2.x → falha de build Cython). O CATE/IVB usa R-learner manual.

numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.6.0
shap==0.51.0
networkx==3.6.1
statsmodels==0.14.6
matplotlib==3.10.9
seaborn==0.13.2
jinja2==3.1.6
nbconvert==7.17.1
nbclient==0.10.4
ipykernel==7.2.0
